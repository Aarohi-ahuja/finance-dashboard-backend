# Environment configuration for Finance Dashboard

# Port for the Express API server
PORT=3000

# Secret for JWT signing. Must align with local dev or production secrets.
JWT_SECRET=super_secret_evaluation_key_123

# Database connection format for SQLite
DATABASE_URL="file:./dev.db"
